---
name: toushi
description: 把 GitHub 项目、本地代码或外部方法做成“双层偷师报告”：先画完整架构与执行链路，逐层解释输入、动作、判断、回退和输出，再逐项验货并改造成用户自己的工作流。适用于既想真正看懂设计、又想借鉴落地的场景。
metadata:
  slug: toushi
  displayName: 偷师
  version: "1.1.0"
---

# 偷师

正式基线：v1.1.0 · 完整链路、轻量交付版。层数、模块数和具体业务依目标源码确定，不套用某次案例的固定数量。

先**看清整套招式怎么设计**，再判断**哪些招式值得化为己用**。它不是“Repo Explainer 加一张验证表”，而是一份完整的双层报告：

1. **解剖层（Repo Explainer）**：外部对象到底怎么运作？入口在哪里？CLI/API 怎么用？模块怎样传递数据？有哪些依赖、限制与失败回退？
2. **化用层（吸星大法）**：这套设计的哪一环在用户业务中成立？它暗含什么前提？该沿用、删掉还是改造成自己的版本？

不能用一张“沿用/删/改”表替代架构解读；也不能用一份漂亮源码报告冒充“已经化为己用”。

## 适用边界

- GitHub 仓、本地代码目录：先看真实源码与实际文件，再判断能否借鉴。
- Skill、课程、文章、视频、PDF：先取得原文，再抽出执行路径与隐藏判断。
- 用户只想安装、运行、修 Bug、做安全审计或单纯读一份 README 时，不使用本 Skill。

## 两个能力如何融合

| 来自 | 只保留的核心 | 在偷师中的位置 |
|---|---|---|
| Repo Explainer | Fetch → Scan → Analyze → Synthesize → Render → Deliver 的完整解剖；包括真实入口、CLI/API、模块、数据流、依赖、限制与 `路径:行号` | **解剖层**：先让用户真正看懂外部对象如何设计 |
| 吸星大法 | 流程萃取、硬验证门、独立画自己、合一、缺口与归档 | **决策层**：决定哪些能进用户体系、如何改 |

**强制规则：**最终交付必须同时保留完整解剖层与化用层。缺少架构、模块或实际 CLI/API，只能称为“借鉴建议”；缺少验证表、独立我的版本和合一，只能称为“源码解读”。

## 一体化工作流

### 执行策略｜内容完整，制作复用

- **默认标准档**：完整执行下述解剖与化用流程；复用 [assets/report-template.html](assets/report-template.html) 的浅色版式和流程图组件，按 [references/fresh-report-style.md](references/fresh-report-style.md) 做必要验收。标准档不是摘要档，不删真实接口、关键分支、失败回退或化用前提。
- **审计档**：仅用户明确要求全面审计、多尺寸验收或逐图截图时启用额外检查；不因“完整版”“拆细一点”自动升级视觉检查。内容真实性与权限边界在两档中一样严格。
- 一个目标只维护一份事实底稿，节点、逐层解释、接口表、HTML 与 Markdown 共用它。不要分别写两份正文再人工对齐；中间文件按需生成，不为了目录好看复制多份。
- 重看同一对象前，按 `来源 + commit/内容哈希 + 实际读取范围 + 用户需求` 核对已有底稿。命中才复用事实；脏工作区要检查相关文件哈希，不能只看 commit。版本变化则读取差异及受影响上下游；旧底稿没覆盖的文件仍须读。业务约束变化要重做化用判断。远端不可访问时明确“沿用某日快照”，不能冒充当前版本。
- 无 Git 版本时，对实际引用的本地源文件计算 SHA-256 并记相对路径；不等用户提供哈希，不扫描无关文件。工具不可用则写明原因，下一次不把该快照视为可直接复用。
- 记录取材、分析、呈现、验收的实际起止时间及失败次数；阶段边界记一次即可，不为计时频繁调用工具。不承诺固定分钟数，不把干跑预测写成提速实测。
- 独立核对只委派给能并行的明确任务，例如接口审查；主代理继续分析。不要让多名代理重复全文拆解、评分或渲染。
- **停止返工**：图形首次失败只做一次有针对性的修复；仍失败立即切换模板内的纵向节点与分支组件。浏览器失败同理：一次恢复后仍失败，保存可读产物并标注未完成的视觉验证，不循环换浏览器或安装工具。不能把失败当通过，也不能为省时丢掉证据链。

### Phase 0｜取材、价值门与边界

先记录来源地址、本地路径或 Git commit、取材日期。GitHub 对象先判断是否值得吸：活跃度、实际内容能否对上用户的业务环节、材料是否有可提炼的判断或回退路径。高 star 不是适配结论。

声明只读：不得运行目标仓的脚本、安装依赖或访问其密钥，除非用户另行授权。

读用户业务文件前，明确范围。默认只读用户点名的文档、配置与脚本；跳过 `.env`、密钥、token、客户名单、个人隐私和原始敏感数据。读不到就标明缺失，不猜。

### Phase 1｜完整解剖：先讲清它怎样设计，再萃取“外部招式图”

按输入类型选择：

- **代码仓或本地代码目录**：保留 `$repo-explainer` 的完整六段逻辑：Fetch（来源、commit、范围与大小限制）→ Scan（目录、语言、清单、依赖与入口候选）→ Analyze（模块、数据对象、真实 CLI/API/SDK 与外部调用）→ Synthesize（白话架构、主流程、局限与术语）→ Render（HTML + Markdown）→ Deliver（证据与核验方式）。每一项非显而易见结论保留 `路径:行号`。
- **纯 Markdown Skill**：不要硬造调用图或软件 API；完整解释其提示词协议：触发语、输入、强制步骤、判断、输出、归档、边界。若同目录有 Python/Shell/Node 脚本，则把真实命令、参数、输入、输出和副作用独立列为 CLI 表。
- **方法材料**：只读正文，抽取输入、动作、判断、循环、回退和输出。

解剖层依次交付：**完整架构总图 → 每层展开 → 代表性请求的端到端链路 → 接口/数据/依赖/局限/术语**。画图前必须读 [references/architecture-report.md](references/architecture-report.md)。

- 总图放在一句话定位之后、所有适配结论之前，贯通入口、执行者、主要层、外部工具、产物和实际终点。节点编号与后文逐层说明互相对应。
- 每层解释：输入、内部动作、为什么这么做、判断条件/回退、产物、交给谁、源码依据。只有模块名或一句职责不算展开。
- 代码调用、提示词要求的协作、报告作者归纳的业务顺序必须明确区分。Skill 集合没有总调度程序时，画明宿主 Agent 的作用；不能把所有模块虚构成自动串行流水线。
- 从真实源文件走通主要输入分支。需要让读者看见“同一个输入怎样被逐步加工”，包含正文中的有条件动作、失败回退和终点；不存在的检查机制注明缺失，不补成原生功能。
- HTML 内直接显示可阅读的架构/流程图，不能只留 Mermaid 源码或文字箭头。保留 Markdown 可编辑图稿；图太密时用总览加子图，不能为简洁删除关键分支。

解剖完成后可加一张**外部招式简图**帮助记忆，但不能替代完整总图与逐层链路。“萃取”是删去模板、例子和重复说明，不是限制必要步骤数量。代码扫描口径见 [references/code-reading.md](references/code-reading.md)。

### Phase 2｜硬门：逐项验货“拿什么、怎么改、什么先别碰”

此阶段读取 [references/adaptation.md](references/adaptation.md)，核对输入、评价标准和默认前提。

这一步不能由 AI 替用户下商业判断。AI 可依据已知约束起草；未知必须标 `〔待用户确认〕`，不能补写。

把它作为解剖层与“我的版本”之间的独立视觉模块：用户先看懂外部系统，再看每一项是否值得带走，最后才进入自己的流程。不得把它塞进报告末尾或与最终行动清单混在一起。

| 对方模块 | 处理 | 你要留下的核心 | 改造后的去处 |
|---|---|---|---|

- **处理**只能用清楚的动作词：`保留`、`保留，但换数据主次`、`改造`、`前置为闸门`、`延后`、`暂不使用`、`删掉`、`后置`。
- 每一行必须同时说明：原模块的价值、用户侧如何落地；不能只写“适合/不适合”。
- 表内只列实际存在的模块；“没有定时器”之类缺失能力放局限或提案，不写成待删除/待沿用的原模块。
- 紧接表格补一段“为什么不照搬整条链”：点出用户当前阶段、缺失前提和误用风险。
- 另加一张简短的“硬前提核对”表，保留原有判断：外部环节、在我这里成立吗、它默认我有什么、我已有的约束或独有条件、沿用/删/改。这样既便于小白做选择，也不丢失严谨的前提审查。

至少要出现一项“删”或“改”。尤其单独审查外部的评分、权重和阈值：它量的是数量还是内容？是否奖励了用户要避免的事？用户真正重视的条件是否缺失？

### Phase 3｜独立画“我本来就在做什么”

完成验证后，**暂时不参考外部招式图**。只读用户已授权的真实业务产物、已确认约束或项目文档，画出用户目前实际在跑的流程。口头计划和真实文件不一致时，如实写出差异。

默认只读用户点名的文档、配置和脚本；不读原始敏感数据。跳过 `.env`、密钥、token、客户名单和个人隐私。没有业务产物时，输出“学习版流程”，明确标为假设演练。

### Phase 4｜合一：化成“我的版本”

将 Phase 1 外部招式和 Phase 3 用户实际流程合并：保留可复用机制，替换不适合的输入、判断标准和风险门。每个改变要能回答“来自外部还是来自我、为什么改”。

最终必须明确：

1. 哪些来自外部，哪些来自用户自己的实际情况；
2. 每个改动的原因；
3. 还缺哪些数据、权限、工具或用户决策。

### Phase 5｜交付、验证与归档

为一次拆解创建一个目录，默认放进用户已有的相关知识库或项目目录，不新建孤立知识库。核心交付如下，源文件与旧报告不覆盖：

```text
<日期>_<对象名>/
  偷师报告.html
  偷师报告.md
  来源与验证.md
```

`来源与验证.md` 记录来源版本、已读范围、未读项、复用依据、阶段用时和实际检查结果；供下次重拆复用。可编辑图稿放 Markdown 中。旧版的 `00` 到 `04` 分拆底稿仅在协作或用户需要时生成，不再默认复制一套。

最终只交付一份**偷师报告**（HTML + Markdown），章节顺序固定为：

1. 一句话结论：值不值得吸、吸什么、不吸什么；
2. **第一层：它究竟怎样设计**（完整架构总图、逐层说明、端到端子流程、全部真实接口、数据、依赖、局限、术语）；
3. 外部招式简图（可选；不得代替第 2 项的完整链路）；
4. **第二层：逐项验货——拿什么、怎么改、什么先别碰**（主验证表 + 硬前提核对）；
5. 用户独立流程；
6. 合一后的“我的版本”；
7. 还缺什么与下一步人工门；
8. 来源与 `路径:行号` 证据。

Repo Explainer 的原始 HTML/Markdown只能作为工作底稿，用户明确要求时才另附。所有市场、销量、成本等数据必须标来源；不确定就写 `〔待验证〕`。

HTML 默认使用清新浅色风格，禁止深蓝或黑色底：背景 `#F7F5EF`，内容卡 `#FFFFFF`，正文 `#263238`，薄荷绿 `#4E8C80`，天青 `#73A8C7`，暖黄 `#E6B85C`。详细样式与检查项见 [references/fresh-report-style.md](references/fresh-report-style.md)。

## 验收

按默认标准档先做内容核验与必要视觉检查，具体次数见样式参考；下列内容条件不能因提速跳过。

- 总图在前，每个主要层能定位到展开说明；读者能沿一次真实请求说清每一步拿到什么、做什么、为什么、下一步去哪里；
- 主要输入分支、回退和终点有源码支撑；作者提出的新流程与原生行为有明确标签；
- 架构解剖层独立完整，且目标实际存在的 CLI/API 没被省略；
- “看懂”结论可回到真实文件、材料或版本验证；
- 验证表存在至少一处删或改，且不编造用户条件；
- “我的版本”不以外部图为模板，能指出用户实际产物与口头计划的差异；
- 最终流程至少有一处是用户自己的，而非外部方案的复述；
- HTML 背景与卡片符合清新浅色风格，解剖与化用两层视觉权重相当；
- 未授权时没有执行外部项目、安装依赖或外传用户资料。
